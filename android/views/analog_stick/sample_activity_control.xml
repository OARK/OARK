<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res/sep_17.fourwwcontrol"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:background="#0099cc"
    tools:context=".ControlActivity">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->
    <TextView android:id="@+id/fullscreen_content" android:layout_width="match_parent"
        android:layout_height="match_parent" android:keepScreenOn="true" android:textColor="#33b5e5"
        android:textStyle="bold" android:textSize="50sp" android:gravity="center"
        android:text="@string/dummy_content" />

    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows. -->
    <FrameLayout android:layout_width="match_parent" android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <LinearLayout android:id="@+id/fullscreen_content_controls" style="?metaButtonBarStyle"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:background="@color/black_overlay" android:orientation="horizontal"
            tools:ignore="UselessParent">

        </LinearLayout>
        <view
            android:layout_width="160dp"
            android:layout_height="160dp"
            class="sep_17.fourwwcontrol.AnalogStickView"
            android:id="@+id/left_analog_stick"
            android:layout_gravity="left|bottom"

            app:pointerColor="#88FF1111"
            app:backColor="#000000"
            app:transparent="false"
            app:pointerRad="100.0"

            app:effects="false"
            app:axes="Y"
        />

        <view
            android:layout_width="160dp"
            android:layout_height="160dp"
            class="sep_17.fourwwcontrol.AnalogStickView"
            android:id="@+id/right_analog_stick"
            android:layout_gravity="right|bottom"

            app:pointerColor="#88FF1111"
            app:backColor="#000000"
            app:transparent="true"
            app:pointerRad="100.0"

            app:effects="true"
            app:axes="X"/>

    </FrameLayout>

</FrameLayout>
